# ===== Edge Node Dependencies =====
# Install: pip install -r requirements.txt
# GPU (CUDA): pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#   then:    pip install -r requirements.txt

# YOLO detection + exports (.pt, .onnx, .engine)
ultralytics>=8.0.0
torch>=2.0.0
torchvision>=0.15.0

# Computer vision
opencv-python>=4.5.0
numpy>=1.22.0
Pillow>=8.0.0

# ByteTrack tracker (via supervision)
supervision>=0.18.0

# Message queue (Redis + Celery)
celery[redis]>=5.3.0
redis>=4.5.0

# HTTP push to central server
requests>=2.28.0

# Local control-plane API
fastapi>=0.100.0
uvicorn[standard]>=0.20.0

# Optional: ONNX Runtime for optimised inference
# onnxruntime-gpu>=1.14.0

# Optional: TensorRT (install separately via NVIDIA)
# tensorrt>=8.6.0